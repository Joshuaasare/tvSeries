import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

interface Props {}
const SeriesEpisodes: React.FC<Props> = ({}) => {
  return (
    <View>
      <Text>SeriesEpisodes</Text>
    </View>
  );
};

export default SeriesEpisodes;

const styles = StyleSheet.create({});
